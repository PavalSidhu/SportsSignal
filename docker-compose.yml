services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: sportssignal
      POSTGRES_PASSWORD: sportssignal
      POSTGRES_DB: sportssignal
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
